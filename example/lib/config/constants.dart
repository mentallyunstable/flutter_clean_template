/// Add any constant values here - links, shared prefs keys, secure storage keys,
/// any debug settings, etc.
/// For example:
/// static const String token = "token";
class AppConstants {
  AppConstants._();
}
