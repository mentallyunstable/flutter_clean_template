/// Store any constant values here - links, shared prefs keys, secure storage keys,
/// any debug settings, etc.
class AppConstants {
  /// Add const values like shown below
  // static const String token = "token";
}
